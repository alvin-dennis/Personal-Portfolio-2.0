---
import { join } from "node:path";
import Navbar from "@/components/Navbar.astro";
import { AstroFont } from "astro-font";
import { SITE_CONFIG } from "@/lib/constants";

const {
  title,
  description,
  siteLogo,
  lang,
  author,
  socialLinks,
  socialImage,
  canonicalURL = new URL(Astro.url.pathname, Astro.site).href,
} = SITE_CONFIG;

const ogImgUrl = canonicalURL + socialImage;

import "@/styles/global.css"
---

<!doctype html>
<html lang={lang} class="scroll-pt-16 scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="author" content={author} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:image" content={ogImgUrl} />

    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={ogImgUrl} />
    <AstroFont
      config={[
        {
          name: "Druk Wide",
          src: [
            {
              style: "normal",
              weight: "900",
              path: join(
                process.cwd(),
                "public",
                "fonts",
                "Druk-Super-Trial.otf"
              ),
            },
          ],
          preload: true,
          display: "swap",
          selector: "h1",
          cacheDir: "./node_modules/.cache/astro-font",
          fallback: "sans-serif",
        },
        {
          name: "Druk Super",
          src: [
            {
              style: "normal",
              weight: "400",
              path: join(
                process.cwd(),
                "public",
                "fonts",
                "DrukText-Super-Trial.otf"
              ),
            },
          ],
          preload: true,
          display: "swap",
          selector: "h2",
          cacheDir: "./node_modules/.cache/astro-font",
          fallback: "sans-serif",
        },
        {
          name: "League Spartan",
          src: [
            {
              style: "normal",
              weight: "400",
              path: join(
                process.cwd(),
                "public",
                "fonts",
                "LeagueSpartan-Regular.ttf"
              ),
            },
          ],
          preload: true,
          display: "swap",
          selector: "p, li, a, span, h4",
          cacheDir: "./node_modules/.cache/astro-font",
          fallback: "sans-serif",
        },
      ]}
    />
  </head>
  <body class="font-poppins flex min-h-screen flex-col bg-white dark:bg-black">
    <main class="mx-auto max-w-3xl flex-1 px-5 pb-28">
      <slot />
    </main>
    <Navbar siteLogo={siteLogo} socialLinks={socialLinks} />
  </body>
</html>
