---
import Card from "./Card/index.astro";
import { EXPERIENCE } from "../lib/constants";
---

<Card
  colSpan="md:col-span-1"
  rowSpan="md:row-span-2 flex gap-4"
  title=" 🤝 Experience"
  href="/experience"
>
  <div class="mt-2 space-y-4">
    {
        EXPERIENCE.slice(0, 1).map((exp) => (
          <div>
            <p class="text-lg text-primary-500">{exp.position}</p>
            <p class="text-sm">{exp.company}</p>
            <p class="text-sm">{exp.start} - {exp.end}</p>
          </div>
        ))
      }
  </div>
</Card>
